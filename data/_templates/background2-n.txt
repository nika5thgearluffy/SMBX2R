fill-color=0x000000 ;Renders behind everything

;Each layer is a parallax layer
[Layer 1]
depth=INFINITE ;Negative values scroll slower, positive values faster than the scene
img="Dummy.png"

;; Uncomment other fields as necessary

; repeatX=false
; repeatY=false

; Overrides scroll properties of "depth"
; fitX=false ; Left edge of layer is at left edge of section and vice-versa
; fitY=false ; Top edge of layer is at top edge of section and vice-versa

; x=0
; y=0

; speedX=0
; speedY=0

; frames=1
; framespeed=8
; startingFrame=1

; Overrides scrolling properties of "depth"
; priority=-100

; color=white
; opacity=1

; alignX=LEFT
; alignY=TOP

; marginLeft=0
; marginRight=0
; marginTop=0
; marginBottom=0

; padX=0
; padY=0

; Overrides scrolling properties of "depth"
; parallaxX=0
; parallaxY=0

; maxParallaxX=0
; maxParallaxY=0

; hidden=false
; debug=false